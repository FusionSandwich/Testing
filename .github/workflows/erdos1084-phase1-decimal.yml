name: Erdős 1084 explicit 2.0465 Phase-I theorem

on:
  pull_request:
    branches: [testing]
    paths:
      - 'erdos1084/formal/Erdos1084/Phase1DecimalTheorem.lean'
      - 'erdos1084/formal/Erdos1084Phase1Decimal*.lean'
      - '.github/workflows/erdos1084-phase1-decimal.yml'
  push:
    branches:
      - 'agent/erdos-1084-phase1-contact-model-lean-20260819'
  workflow_dispatch:

jobs:
  decimal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject placeholders and project axioms
        run: |
          if grep -R --include='*.lean' -nE '(^|[^[:alnum:]_])(sorry|admit|sorryAx)([^[:alnum:]_]|$)' erdos1084/formal; then exit 1; fi
          if grep -R --include='*.lean' -nE '^[[:space:]]*(axiom|constant)[[:space:]]+' erdos1084/formal/Erdos1084; then exit 1; fi
      - name: Install Elan
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Build and audit explicit theorem
        working-directory: erdos1084/formal
        run: |
          lake update
          lake exe cache get
          lake build Erdos1084.Phase1DecimalTheorem
          lake env lean Erdos1084Phase1DecimalTests.lean
          lake env lean Erdos1084Phase1DecimalAxiomAudit.lean
