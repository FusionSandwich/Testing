name: Erdős 1084 Phase-I boundary measure subadditivity

on:
  pull_request:
    branches: [testing]
    paths:
      - 'erdos1084/formal/Erdos1084/Phase1Boundary*.lean'
      - 'erdos1084/formal/Erdos1084Phase1Boundary*.lean'
      - '.github/workflows/erdos1084-phase1-boundary-measure.yml'
  push:
    branches:
      - 'agent/erdos-1084-phase1-contact-model-lean-20260819'
  workflow_dispatch:

jobs:
  boundary-measure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Reject placeholders and explicit project axioms
        run: |
          if grep -R --include='*.lean' -nE '(^|[^[:alnum:]_])(sorry|admit|sorryAx)([^[:alnum:]_]|$)' erdos1084/formal; then exit 1; fi
          if grep -R --include='*.lean' -nE '^[[:space:]]*(axiom|constant)[[:space:]]+' erdos1084/formal/Erdos1084; then exit 1; fi
      - name: Install Elan
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh | sh -s -- -y --default-toolchain none
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
      - name: Build and audit
        working-directory: erdos1084/formal
        run: |
          lake update
          lake exe cache get
          lake build Erdos1084.Phase1BoundaryGeometry
          lake build Erdos1084.Phase1BoundaryMeasure
          lake env lean Erdos1084Phase1BoundaryTests.lean
          lake env lean Erdos1084Phase1BoundaryMeasureTests.lean
          lake env lean Erdos1084Phase1BoundaryAxiomAudit.lean
          lake env lean Erdos1084Phase1BoundaryMeasureAxiomAudit.lean
