name: AFP Lean Gate 3

# Gate 3 is the current superset mathematics check. It runs once per coherent
# pull-request state and cancels obsolete runs when the branch advances.
on:
  pull_request:
    branches:
      - testing
    paths:
      - 'afp_barrier_gate1/AFPBarrier/**'
      - 'afp_barrier_gate1/AFPBarrier.lean'
      - 'afp_barrier_gate1/README.md'
      - 'afp_barrier_gate1/GATE3.md'
      - 'afp_barrier_gate1/gate3/**'
      - 'afp_barrier_gate1/lakefile.lean'
      - 'afp_barrier_gate1/lean-toolchain'
      - 'afp_barrier_gate1/lake-manifest.json'
      - '.github/workflows/afp-barrier-gate3.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  mathematics:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: afp_barrier_gate1

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up deterministic Python runtime
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Deterministic all-orders product-grid audit
        shell: bash
        run: |
          set -euo pipefail
          python gate3/equal_angle_product_audit.py \
            --output-dir gate3/generated 2>&1 | tee gate3-deterministic-audit.log

      - name: Install Elan and pinned Lean toolchain
        working-directory: .
        shell: bash
        run: |
          set -euo pipefail
          curl -sSfL https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
            | sh -s -- -y --default-toolchain "$(cat afp_barrier_gate1/lean-toolchain)"
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"

      - name: Record toolchain versions
        shell: bash
        run: |
          set -euo pipefail
          {
            echo "commit=${GITHUB_SHA}"
            python --version
            lean --version
            lake --version
          } | tee gate3-toolchain.log

      - name: Resolve pinned dependencies
        shell: bash
        run: |
          set -euo pipefail
          lake update 2>&1 | tee gate3-lake-update.log

      - name: Download Mathlib compiled cache
        shell: bash
        run: |
          set -euo pipefail
          lake exe cache get 2>&1 | tee gate3-mathlib-cache.log

      - name: Lean kernel build
        shell: bash
        run: |
          set -euo pipefail
          lake build 2>&1 | tee gate3-lean-build.log

      - name: Axiom audit
        shell: bash
        run: |
          set -euo pipefail
          lake env lean AFPBarrier/AxiomAudit.lean 2>&1 | tee gate3-axiom-audit.log
          if grep -q 'sorryAx' gate3-axiom-audit.log; then
            echo "A theorem depends on Lean's sorry axiom." >&2
            exit 1
          fi

      - name: Source placeholder audit
        shell: bash
        run: |
          set -euo pipefail
          if grep -RInE --include='*.lean' '(^|[^[:alnum:]_])(sorry|admit)([^[:alnum:]_]|$)' AFPBarrier AFPBarrier.lean; then
            echo "Proof placeholder found." >&2
            exit 1
          fi
          if grep -RInE --include='*.lean' '^[[:space:]]*axiom([[:space:]]|$)' AFPBarrier AFPBarrier.lean; then
            echo "User axiom declaration found." >&2
            exit 1
          fi
          echo "No proof placeholders or user axiom declarations found." | tee gate3-source-audit.log

      - name: Write Gate 3 report
        if: always()
        shell: bash
        run: |
          {
            echo "AFP Barrier Gate 3 mathematics validation"
            echo "commit=${GITHUB_SHA}"
            echo "run_id=${GITHUB_RUN_ID}"
            echo "run_attempt=${GITHUB_RUN_ATTEMPT}"
            echo "toolchain=$(cat lean-toolchain)"
            echo "timestamp_utc=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          } > gate3-report.txt

      - name: Pack exact source snapshot
        if: always()
        working-directory: .
        shell: bash
        run: |
          set -euo pipefail
          tar \
            --exclude='afp_barrier_gate1/.lake' \
            --exclude='afp_barrier_gate1/*.log' \
            --exclude='afp_barrier_gate1/gate3-report.txt' \
            -czf afp-barrier-gate3-source-${GITHUB_SHA}.tar.gz \
            afp_barrier_gate1 \
            .github/workflows/afp-barrier-gate3.yml

      - name: Upload verification logs and source snapshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: afp-lean-gate3-${{ github.run_id }}
          path: |
            afp_barrier_gate1/gate3-report.txt
            afp_barrier_gate1/gate3-toolchain.log
            afp_barrier_gate1/gate3-deterministic-audit.log
            afp_barrier_gate1/gate3/generated/equal_angle_product_audit.md
            afp_barrier_gate1/gate3/generated/equal_angle_product_audit.csv
            afp_barrier_gate1/gate3-lake-update.log
            afp_barrier_gate1/gate3-mathlib-cache.log
            afp_barrier_gate1/gate3-lean-build.log
            afp_barrier_gate1/gate3-axiom-audit.log
            afp_barrier_gate1/gate3-source-audit.log
            afp_barrier_gate1/lake-manifest.json
            afp-barrier-gate3-source-*.tar.gz
          if-no-files-found: warn
