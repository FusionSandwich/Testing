# Temporary Lean verification package

This isolated package verifies finite and arithmetic cores from the Erdős–Gyárfás proof search. It is staged on a dedicated branch only because GitHub Actions in the private target repository was blocked before runner allocation by the account billing/spending-limit state.
