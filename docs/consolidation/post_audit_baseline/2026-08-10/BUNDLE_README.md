# AFP post-audit provenance baseline

This directory fixes the authoritative pre-repair state. It contains no mathematical repair and changes no theorem statement. Start with `AUTHORITATIVE_STATE.md`, then use the JSON/CSV manifests for machine checks.

The branch was created from the strongest valid live descendant before these files were committed. `POST_COMMIT_VERIFICATION.md` is added after the first baseline commit is reconstructed independently.
