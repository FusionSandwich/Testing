# Prompt 4 reacceptance after richer Prompt 3 reconciliation

## Historical baseline

Prompt 4 was already complete and accepted before this reconciliation:

```text
Prompt 4 implementation:
    ae5b4c7635f917ea7abb6feb58717cc0173b4cd7
Prompt 4 theorem merge:
    d1a31d195ae3c371b3f9cee29bd7ed34d1ff0994
Prompt 4 integration merge:
    9f8f273b1f592801aec0cc4794cf5f4688eb836f
Prompt 4 final exact-head target:
    94aebf6578a43516cce4bb7c042fc57681c93890
Prompt 4 final target tree:
    872828f5099aeb7df4e0ab871b9e881de4054a97
```

The richer Prompt 3 package is integrated additively after that history. No accepted Prompt 4 theorem source, remainder constant, extremal definition, feasible-cone formula, or reduced-ring statement is replaced.

## Preserved exact theorems

For every integer `N>=2`, the accepted product-grid estimates remain

```text
0 <= r_polar(N)
     -[
        (8/pi^4)N^4
        +(10/(3pi^2))N^2
        +13/45
      ]
  <= pi^2/(48N^2),
```

and

```text
0 <= Q_pole(N)
     -[
        N^2/pi^2
        +7/12
      ]
  <= pi^2/(12N^2).
```

The accepted finite algebra also retains:

```text
inward meridional polar rate = 1/(2 sin^2 h);
each azimuthal polar rate    = 1/(4 sin^4 h);
sharp unreduced graph-class leading constant = 8/pi^4;
epsilon_i<=C h^2 => r_i>=4/(C h^2);
E_K>=4/(RK);
C*>=4/R;
fixed-K minimizer existence;
strict large-order exclusion of the unreduced product family
from linear-rate extremal classes;
projective feasible-cone formula for Q;
fixed-mean sliced LP and explicit dual anisotropy certificate;
sharp equality examples;
stated biregular/perfect-matching reduced-ring obstruction.
```

No ring-symmetry assumption is introduced into the fixed-graph polar forcing theorem.

## Preserved bounded statuses

```text
Delsarte/Gegenbauer: BLOCKED;
BakryЃmery: REJECTED for Prompt 4;
compact homogeneous spaces: DEFERRED;
discrete transport metrics: DEFERRED;
general reduced-ring split/merge obstruction: DEFERRED;
formal discrete geometry: bounded to the accepted finite algebra.
```

The reduced-ring theorem remains limited to the stated biregular/perfect-matching coupling class.

## Combined-head regression contract

Every final workflow reruns:

```text
pure_math/barriers/prompt4_sharp_barrier_audit.py;
AFPBarrier/SharpProductBarriers.lean;
the full aggregate Lean build;
the focused axiom audit;
placeholder and singular/plural user-axiom scans;
independent nanoda checking of the Prompt 4 finite declarations;
archive equality;
pure-math-only path scope;
generated-artifact rejection;
git diff --check.
```

The exact audit verifies polar coefficient extraction, rigorous remainder constants, uniquely forced fixed-graph rates, the universal barrier and loss-window transfer, extremal separation, feasible-cone anisotropy, sharp examples, and the stated reduced-ring impossibility certificate.

## Final synthesis treatment

The final hierarchy is unchanged:

```text
central candidate:
    sampled quadratic covariance factorization and structural rigidity;

strong companion:
    restricted global Q=1 classification and explicit graph-global
    near-rigidity;

supporting theory:
    Prompt 1 feasibility/shared-edge duality,
    exact examples and counterexamples,
    Prompt 4 sharp product-grid barriers, extremal theory,
    and feasible-cone duality.
```

The synthesis states the true repository history: Prompt 4 was already closed on the accepted target; the richer Prompt 3 package was integrated additively afterward; Prompt 4 theorem statements were preserved and revalidated on the combined exact head.

## Dynamic reacceptance metadata

The literal combined implementation SHA/tree, six implementation-head workflow run/job identifiers, Prompt 4 evidence artifact ID and SHA-256 digest, integration method, actual final target SHA/tree, six post-integration workflow identifiers, and final review/preservation records are recorded in the authoritative acceptance comment on the clean final reconciliation PR. That comment is incorporated here by reference.
